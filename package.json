{
  "name": "selector-like-chrome-dev",
  "version": "1.0.0",
  "description": "$, $$, $x like chrome-dev.",
  "main": "lib/main.js",
  "scripts": {
    "build": "babel src/main.js -o lib/main.js -s inline",
    "watch": "babel src/main.js -o lib/main.js -s inline -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "selector",
    "css",
    "xpath"
  ],
  "author": "3846masa",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.8.23",
    "babel-eslint": "^4.1.3",
    "eslint": "^1.6.0"
  }
}
